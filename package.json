{
  "name": "string-prototype-replace-regexp-benchmark",
  "private": true,
  "scripts": {
    "bench": "./bench.sh",
    "build": "node build.js"
  },
  "devDependencies": {
    "emoji-test-regex-pattern": "^2.2.0",
    "jsesc": "^3.0.2"
  }
}
